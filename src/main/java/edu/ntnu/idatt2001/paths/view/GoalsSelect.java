package edu.ntnu.idatt2001.paths.view;

import javafx.scene.control.Button;
import javafx.scene.layout.Region;
import javafx.util.Builder;

public class GoalsSelect implements Builder<Region> {

    //TODO: kommer hit fra NewGame siden om man trykker paa feks Configure Custom Goals.
    //Lykke til :*
    @Override
    public Region build() {
        return new Region();
    }
}
